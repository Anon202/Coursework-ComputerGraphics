#include "intersection.h"
#include <iostream>

using namespace std;

int main()
{
	int enter;

	cout << "Test if point is in front of plane (should be true) " << endl;



		//(const Vector3& point,		const Vector3& pointOnPlane, const Vector3& planeNormal);
	//bool a = IsPointInFrontOfPlane();
	cin >> enter;
	return 0;
}